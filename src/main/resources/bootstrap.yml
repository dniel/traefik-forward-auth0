endpoints:
  prometheus:
    sensitive: false

  health:
    enabled: true
    sensitive: false
    details-visible: ANONYMOUS
    disk-space:
      enabled: false
    discovery-client:
      enabled: false

micronaut:
  application:
    name: forwardauth

  security:
    enabled: true

  metrics:
    enabled: true
    export:
      prometheus:
        enabled: true
        step: PT1M
        descriptions: true